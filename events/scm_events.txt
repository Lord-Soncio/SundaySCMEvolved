namespace = scmgeneral #for general triggering events

country_event = {
    id = scmgeneral.1 #Event for giving generals exp
    hide_window = yes
    is_triggered_only = yes

    immediate = {
        every_owned_leader = {
            limit = { leader_class = general }
            add_experience = 1
        }
    }
}